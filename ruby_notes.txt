para crear una nueva app
rails new APP_NAME

bundler --> va a rubygems e instala las gemas

cuando se hace una peticion http, llega al router que lo dirige al controlador dependiendo del tipo de request y el controlador renderiza una vista y redirige a la pagina adecuada o el controlador va al modelo, el modelo se comunica con la db, devuelve la info al modelo y el controlador renderiza una vista

en assets se guardan las imagenes, js, stylesheets.

helpers son sólo para views

sqlite3 tiene problemas --> se usa 1.3.13
para crear rutas --> get 'welcome', to: 'welcome#home' (get 'ruta', to: 'controlador#accion' )
para crear el controlador se agrega un archivo CONTROLLERNAME_controller.rb

class ControladorController < ApplicationController
    def accion
        
    end
end

luego se crea una carpeta en views con el nombre del controlador y dentro de este se crean los archivos .html.erb con el nombre de la accion

para crear links
<%= link_to 'Sobre nosotros', about_path %>
<%= link_to 'text to display', PREFIX_path %>

el igual se debe poner para renderizar.